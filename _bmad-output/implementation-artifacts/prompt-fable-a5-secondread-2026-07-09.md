# Prompt for Fable — SCP Addendum 5 second-read (2026-07-09)

> Copy everything below the line into a fresh CLI session (the non-authoring reader; John drafted A5, so you are the independent second-read — the same discipline that caught 17.33a and the fabricated Yoruba rules).

---

You are the **non-authoring second reader** for **SCP Addendum 5** (the last queued addendum in the SCP 2026-04-15 chain). PM John drafted it; you did not. Your job is the adversarial second-read + a scope pressure-test of the four PO decisions — **not** to fold, not to open A6, not to ratify. Verify from source, not from John's prose. Bounded to A5 per Agreement 28 (TA-28): review A5 and its fold targets only; do not re-litigate the consolidated story files' existing (already §I.1-verified) content.

## Context (one paragraph)

The SCP 2026-04-15 amendment chain A1–A4 is consolidated and §I.1-verified; story files are the single truth. A5 opened per Agreement 30 to route the SQ-1 engine track's **Part-2 handoff** — a three-species reconciliation framework (A proven / B projected / C rewound), each finding terminating in a specific payslip ask. A5 introduces an **evidence-graded finding model** (extends the shipped 17f.2 provenance concept from balances to findings), a **blocking transfer-continuity guard** on 17.5, and two proactive detection layers — all as **appends** to consolidated files, never rewrites. Everything reproduces at frozen catalog pin **667ebdd8**.

## Read in this order

1. `_bmad-output/planning-artifacts/scp-addendum-5-2026-07-09-DRAFT.md` — the draft under review (frontmatter carries the discipline: P##-keyed, appends-not-rewrites, stop-guard).
2. `_bmad-output/implementation-artifacts/sq1-track-handoff-to-bmad-2026-07-05.md` — A5's single evidence input; **§9 is the P1–P9 key table** (John appended; §0–§8 frozen as the SQ-1 track delivered).
3. `_bmad-output/planning-artifacts/scp-consolidation-ledger.md` — **§H** (the A5 queue this fulfils), **§C** (FR registry — confirm FR117/FR118 don't collide with FR103–116), **§A** rows for the five fold targets, **§F** step-5 tracker.
4. The five consolidated fold-target files (confirm A5 would **append**, not rewrite): `17-5-…`, `17-4-…`, `17-3b-…`, `17f-2-…`, `17-16-…` in `implementation-artifacts/`.
5. If checking the arithmetic: `docs/Car_Loan/analysis/reports/AG-Projection-Worklist-2026-07-05.md` (Kolade, Species B) + `AG-Transfer-Restatement-Worklist-2026-07-05.md` (Oke, Species C); detectors `scripts/legacy-report/projection-register.ts` + `transfer-restatement.ts`.

## Verification checklist (A5 §7 — verify each, cite evidence)

1. **P## routing:** every A5 item cites a P##, and each P1–P9 routes **exactly once** (no dual-route). Enumerate P1–P9 and confirm.
2. **Appends, not rewrites:** the five §6 fold rows are appends to existing chains; **no consolidated story file is edited by A5**. Confirm A5 touches zero story-file bodies.
3. **P4 blocking AC coherence:** the transfer-continuity guard is consistent with 17.5's existing placeholder AC + `SPRINT-BLOCKED` banner, and with the W2 schema (`loan_mda_reassignments` / `person_loans`). The arithmetic guard (balance jumps ~Δ×monthly) matches `transfer-restatement.ts`.
4. **Scope:** R-1…R-4 recommendations and the A5 items stay **inside Line-2-signed scope** (reconciliation detection is signed). Flag anything that reads as *new authority* (e.g. anything that would post an estimate to the ledger, or auto-act on a projected finding).
5. **FR registry:** FR117 (evidence-graded findings) + FR118 (structured payroll-request worklists) are correctly next-free after FR116; no collision.
6. **Stop-guard:** A5 correctly declares itself the last queued addendum; A6 stays closed.

## Pressure-test the four PO decisions (R-1…R-4) — endorse or flag, do NOT decide

R-1…R-4 are **judgment calls that return to Awwal (PO)** — you do not ratify them. For each, give one line: **ENDORSE** John's recommendation, or **FLAG** a specific concern (scope, honesty/non-punitive, or a cheaper/safer design). This gives the PO your adversarial read before they commit; the decision stays theirs.
- R-1 proactive-vs-on-demand · R-2 threshold governance (config vs code) · R-3 collector registry (existing schema vs net-new) · R-4 do B/C findings post to the ledger.

## Return (your "final submission")

1. **Verdict:** PASS (ready to fold) **or** CORRECTIONS REQUIRED (enumerate; TA-28 bounded — corrections only, no new scope).
2. **Checklist results** (1–6 above), each PASS/FLAG with file:line evidence.
3. **R-1…R-4:** one ENDORSE/FLAG line each (for the PO).
4. If PASS: the **confirmed §6 fold-row list** (the exact appends Bob/John will apply), so the fold matches what you verified with zero drift.
5. Do **not** fold, do **not** edit A5 or any consolidated file, do **not** open A6. Route testable disagreements to a falsification test; judgment calls to Awwal.

**On completion:** the PO (Awwal) brings your submission back to the John session with their R-1…R-4 ratification; John applies any bounded corrections (you diff-re-read if so, §G.1-style), records the ratification into A5 §5, then the A5 §6 rows are folded and the chain-closed marker lands in ledger §F.
